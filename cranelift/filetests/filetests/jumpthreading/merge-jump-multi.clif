test jump-threading precise-output
target x86_64

function %jump_with_arg(i64) -> i64 {
block0(v0: i64):
    jump block1

block1:
    v1 = iconst.i64 0
    jump block2(v1)

block2(v2: i64):
    jump block3(v0)

block3(v3: i64):
    return v3
}

; function %jump_with_arg(i64) -> i64 fast {
; block0(v0: i64):
;     v3 -> v0
;     v1 = iconst.i64 0
;     v2 -> v1
;     return v3
; }

